# Decisions

## 2026-08-02

- 指定緊急避難場所と指定避難所を別のモードで表示し、同じ「避難所」として混ぜない。
- 指定緊急避難場所は法定8災害種別を一つ選び、対象として指定された場所だけへ絞る。
- 全国198,595件を一度に読まず、索引と選んだ都道府県のデータだけを読む。
- 現在地は明示操作後に一度だけ取得し、端末内の直線距離計算だけに使う。保存、送信、URL表示をしない。
- 近い順は安全な経路を示さず、現在開設中の施設一覧でもないことを検索画面とコピー文へ残す。
- 候補は最大6件を端末内だけに保存し、平時の確認用としてコピーできるようにする。
- アカウント、外部解析、広告、Cookieは導入しない。共同計画や端末間同期の実需要が確認できた場合だけ認証を検討する。
- プラットフォームは共通基盤のCloudflare Workers、Hono JSX、Vite+を使う。
